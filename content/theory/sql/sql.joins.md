# SQL: соединения таблиц

> **ЗАГЛУШКА — текст ещё не написан.**
>
> Теория в Dojo пишется человеком или конспектируется из официальной
> документации. Генерировать её моделью запрещено: LLM здесь объясняет уже
> написанное, а не сочиняет учебник (anti-goal №1).
>
> Целевой объём — 800–1500 слов. Заполняется по ходу Фазы 1; сам конспект
> и есть часть учёбы.

## Что нужно уметь после этого узла

- Выбрать тип соединения по задаче и обосновать выбор, а не перебирать наугад
- Объяснить, почему условие на правую таблицу в WHERE превращает LEFT JOIN в INNER
- Написать anti-join через NOT EXISTS и сказать, чем он лучше NOT IN при наличии NULL
- Оценить, во сколько строк развернётся соединение при связи многие-ко-многим
- Применить self-join для сравнения строк одной таблицы между собой

## Откуда это конспектируется

- PostgreSQL 16 Documentation, ch. 7 «Queries» — https://www.postgresql.org/docs/16/queries.html
- PostgreSQL 16 Documentation, ch. 9 «Functions and Operators» — https://www.postgresql.org/docs/16/functions.html

## Разделы к заполнению

### 1. Выбрать тип соединения по задаче и обосновать выбор, а не перебирать наугад

_(пусто)_

### 2. Объяснить, почему условие на правую таблицу в WHERE превращает LEFT JOIN в INNER

_(пусто)_

### 3. Написать anti-join через NOT EXISTS и сказать, чем он лучше NOT IN при наличии NULL

_(пусто)_

### 4. Оценить, во сколько строк развернётся соединение при связи многие-ко-многим

_(пусто)_

### 5. Применить self-join для сравнения строк одной таблицы между собой

_(пусто)_

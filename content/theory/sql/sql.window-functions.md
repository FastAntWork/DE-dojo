# SQL: оконные функции

> **ЗАГЛУШКА — текст ещё не написан.**
>
> Теория в Dojo пишется человеком или конспектируется из официальной
> документации. Генерировать её моделью запрещено: LLM здесь объясняет уже
> написанное, а не сочиняет учебник (anti-goal №1).
>
> Целевой объём — 800–1500 слов. Заполняется по ходу Фазы 1; сам конспект
> и есть часть учёбы.

## Что нужно уметь после этого узла

- Объяснить, чем окно отличается от группировки: почему строки не схлопываются
- Прочитать frame clause и предсказать результат для ROWS против RANGE при дублях в ORDER BY
- Выбрать между ROW_NUMBER, RANK и DENSE_RANK по условию задачи
- Собрать скользящее среднее и накопительный итог, не сползая на подзапросы
- Найти последнее значение в группе через LAST_VALUE и объяснить, почему без явного frame оно возвращает текущую строку

## Откуда это конспектируется

- PostgreSQL 16 Documentation, ch. 7 «Queries» — https://www.postgresql.org/docs/16/queries.html
- PostgreSQL 16 Documentation, ch. 9 «Functions and Operators» — https://www.postgresql.org/docs/16/functions.html

## Разделы к заполнению

### 1. Объяснить, чем окно отличается от группировки: почему строки не схлопываются

_(пусто)_

### 2. Прочитать frame clause и предсказать результат для ROWS против RANGE при дублях в ORDER BY

_(пусто)_

### 3. Выбрать между ROW_NUMBER, RANK и DENSE_RANK по условию задачи

_(пусто)_

### 4. Собрать скользящее среднее и накопительный итог, не сползая на подзапросы

_(пусто)_

### 5. Найти последнее значение в группе через LAST_VALUE и объяснить, почему без явного frame оно возвращает текущую строку

_(пусто)_

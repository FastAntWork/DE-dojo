# SQL: выборка, фильтрация, сортировка

> **ЗАГЛУШКА — текст ещё не написан.**
>
> Теория в Dojo пишется человеком или конспектируется из официальной
> документации. Генерировать её моделью запрещено: LLM здесь объясняет уже
> написанное, а не сочиняет учебник (anti-goal №1).
>
> Целевой объём — 800–1500 слов. Заполняется по ходу Фазы 1; сам конспект
> и есть часть учёбы.

## Что нужно уметь после этого узла

- Прочитать запрос и назвать порядок вычисления его частей: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT
- Объяснить, почему псевдоним из SELECT нельзя использовать в WHERE, но можно в ORDER BY
- Предсказать поведение сравнения с NULL и выбрать IS NULL вместо = NULL
- Отличить DISTINCT от GROUP BY и сказать, когда они дают разный результат

## Откуда это конспектируется

- PostgreSQL 16 Documentation, ch. 7 «Queries» — https://www.postgresql.org/docs/16/queries.html
- PostgreSQL 16 Documentation, ch. 9 «Functions and Operators» — https://www.postgresql.org/docs/16/functions.html

## Разделы к заполнению

### 1. Прочитать запрос и назвать порядок вычисления его частей: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT

_(пусто)_

### 2. Объяснить, почему псевдоним из SELECT нельзя использовать в WHERE, но можно в ORDER BY

_(пусто)_

### 3. Предсказать поведение сравнения с NULL и выбрать IS NULL вместо = NULL

_(пусто)_

### 4. Отличить DISTINCT от GROUP BY и сказать, когда они дают разный результат

_(пусто)_
